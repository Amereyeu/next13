---
title: "Building a markdown post"
description: "Build a serverless 'hello-world' QR code app with Python and AWS."
date: "2021-08-14"
tags: ["mdx", "javascript"]
---

# heading

This is my **first** blog post using _markdown_.

## My subheading

Here is an image:

![Laptop on Desk](/images/linked_list.jpg)

### code snippet

There is a `code snippet` below:

```jsx
export function myComponent() {
  return <p>test</p>;
}
```

### component inserted

And here is our button:

<Button text="my button" />

### table

And here is a table:

| Col 1 | Col 2 |
| ----- | ----- |
| val 1 | val 2 |
| val 3 | val 4 |

### code snippet

```python
print("Hello, World!")
```

### video

<Yt id="gCbGmAFqLoU" />

### links

[Amerey](https://amerey.eu)

[another link](https://seznam.cz)

$\underbrace{a*0+a_1+a_2+\cdots+a_n}*{x}$

$\overbrace{a_0+a_1+a_2+\cdots+a_n}^{x}$

# Markdown Math

## Dots

|   Symbol | Command  |
| -------: | -------- |
|  $\cdot$ | `\cdot`  |
| $\cdots$ | `\cdots` |
| $\ddots$ | `\ddots` |
|  $\dots$ | `\dots`  |
| $\vdots$ | `\vdots` |

## Accents

|         Symbol | Command     |
| -------------: | ----------- |
|    $\acute{a}$ | `\acute`    |
|    $\breve{a}$ | `\breve`    |
|    $\check{a}$ | `\check`    |
|     $\ddot{a}$ | `\ddot`     |
|      $\dot{a}$ | `\dot`      |
|    $\grave{a}$ | `\grave`    |
|      $\hat{a}$ | `\hat `     |
| $\mathring{a}$ | `\mathring` |
|    $\tilde{a}$ | `\tilde`    |

## Operators

|             Symbol | Command            |           Symbol | Command          |
| -----------------: | ------------------ | ---------------: | ---------------- |
|           $\amalg$ | `\amalg`           |        $\otimes$ | `\otimes`        |
|             $\ast$ | `\ast`             |            $\pm$ | `\pm`            |
|         $\bigcirc$ | `\bigcirc`         |           $\rhd$ | `\rhd`           |
| $\bigtriangledown$ | `\bigtriangledown` |      $\setminus$ | `\setminus`      |
|   $\bigtriangleup$ | `\bigtriangleup`   |         $\sqcap$ | `\sqcap`         |
|             $\Box$ | `\Box`             |         $\sqcup$ | `\sqcup`         |
|          $\bullet$ | `\bullet`          |    $\sqrt[n]{x}$ | `\sqrt[n]{x}`    |
|             $\cap$ | `\cap`             |       $\sqrt{x}$ | `\sqrt`          |
|            $\cdot$ | `\cdot`            |          $\star$ | `\star`          |
|            $\circ$ | `\circ`            |         $\times$ | `\times`         |
|             $\cup$ | `\cup`             |  $\triangledown$ | `\triangledown`  |
|          $\dagger$ | `\dagger`          |  $\triangleleft$ | `\triangleleft`  |
|         $\ddagger$ | `\ddagger`         | $\triangleright$ | `\triangleright` |
|         $\diamond$ | `\diamond`         |         $\unlhd$ | `\unlhd`         |
|         $\Diamond$ | `\Diamond`         |         $\unrhd$ | `\unrhd`         |
|             $\div$ | `\div`             |         $\uplus$ | `\uplus`         |
|             $\lhd$ | `\lhd`             |           $\vee$ | `\vee`           |
|              $\mp$ | `\mp`              |         $\wedge$ | `\wedge`         |
|            $\odot$ | `\odot`            |            $\wr$ | `\wr`            |
|          $\ominus$ | `\ominus`          |            $a_x$ | `_x`             |
|           $\oplus$ | `\oplus`           |        $a^{xyz}$ | `^{xyz}`         |
|          $\oslash$ | `\oslash`          |            $a^x$ | `^x`             |
|                    |                    |      $x^{\circ}$ | `^{\\circ}`      |

## Relations

|      Symbol | Command     |        Symbol | Command       |
| ----------: | ----------- | ------------: | ------------- |
|   $\approx$ | `\approx`   |        $\neq$ | `\neq`        |
|    $\asymp$ | `\asymp`    |         $\ni$ | `\ni`         |
|   $\bowtie$ | `\bowtie`   |   $\parallel$ | `\parallel`   |
|   $\bumpeq$ | `\bumpeq`   |       $\perp$ | `\perp`       |
|     $\cong$ | `\cong`     |       $\prec$ | `\prec`       |
|    $\dashv$ | `\dashv`    |     $\preceq$ | `\preceq`     |
|    $\doteq$ | `\doteq`    |     $\propto$ | `\propto`     |
|    $\equiv$ | `\equiv`    |        $\sim$ | `\sim`        |
|    $\frown$ | `\frown`    |      $\simeq$ | `\simeq`      |
|       $\ge$ | `\ge`       |      $\smile$ | `\smile`      |
|       $\gg$ | `\gg`       |   $\sqsubset$ | `\sqsubset`   |
|       $\in$ | `\in`       | $\sqsubseteq$ | `\sqsubseteq` |
|       $\le$ | `\le`       |   $\sqsupset$ | `\sqsupset`   |
|       $\ll$ | `\ll`       | $\sqsupseteq$ | `\sqsupseteq` |
|      $\mid$ | `\mid`      |     $\subset$ | `\subset`     |
|   $\models$ | `\models`   |   $\subseteq$ | `\subseteq`   |
| $\supseteq$ | `\supseteq` |       $\succ$ | `\succ`       |
|    $\vdash$ | `\vdash`    |     $\succeq$ | `\succeq`     |

## Negations

|        Symbol | Command       |          Symbol | Command         |
| ------------: | ------------- | --------------: | --------------- |
|       $\gneq$ | `\gneq`       |     $\not\cong$ | `\not\cong`     |
|      $\gneqq$ | `\gneqq`      |    $\not\equiv$ | `\not\equiv`    |
|       $\lneq$ | `\lneq`       |       $\not\ge$ | `\not\ge`       |
|      $\lneqq$ | `\lneqq`      |       $\not\le$ | `\not\le`       |
|      $\lnsim$ | `\lnsim`      | $\not\parallel$ | `\not\parallel` |
|      $\ncong$ | `\ncong`      |      $\not\sim$ | `\not\sim`      |
|       $\ngtr$ | `\ngtr`       |         $\not<$ | `\not<`         |
|       $\nleq$ | `\nleq`       |         $\not=$ | `\not=`         |
|      $\nless$ | `\nless`      |         $\not>$ | `\not>`         |
|       $\nmid$ | `\nmid`       |    $\nparallel$ | `\nparallel`    |
| $\not\approx$ | `\not\approx` |         $\nsim$ | `\nsim`         |

## Arrows

|                Symbol | Command               |                    Symbol | Command                   |
| --------------------: | --------------------- | ------------------------: | ------------------------- |
|          $\downarrow$ | `\downarrow`          |         $\Longrightarrow$ | `\Longrightarrow`         |
|          $\Downarrow$ | `\Downarrow`          |                 $\mapsto$ | `\mapsto`                 |
|               $\gets$ | `\gets`               |                $\nearrow$ | `\nearrow`                |
|      $\hookleftarrow$ | `\hookleftarrow`      |                $\nwarrow$ | `\nwarrow`                |
|     $\hookrightarrow$ | `\hookrightarrow`     |      $\overleftarrow{AB}$ | `\overleftarrow{AB}`      |
|            $\leadsto$ | `\leadsto`            | $\overleftrightarrow{AB}$ | `\overleftrightarrow{AB}` |
|          $\leftarrow$ | `\leftarrow`          |     $\overrightarrow{AB}$ | `\overrightarrow{AB}`     |
|          $\Leftarrow$ | `\Leftarrow`          |             $\rightarrow$ | `\rightarrow`             |
|      $\leftharpoonup$ | `\leftharpoonup`      |             $\Rightarrow$ | `\Rightarrow`             |
|     $\leftrightarrow$ | `\leftrightarrow`     |       $\rightharpoondown$ | `\rightharpoondown`       |
|     $\Leftrightarrow$ | `\Leftrightarrow`     |         $\rightharpoonup$ | `\rightharpoonup`         |
|      $\longleftarrow$ | `\longleftarrow`      |      $\rightleftharpoons$ | `\rightleftharpoons`      |
|      $\Longleftarrow$ | `\Longleftarrow`      |                $\searrow$ | `\searrow`                |
| $\longleftrightarrow$ | `\longleftrightarrow` |                $\swarrow$ | `\swarrow`                |
| $\Longleftrightarrow$ | `\Longleftrightarrow` |                     $\to$ | `\to`                     |
|         $\longmapsto$ | `\longmapsto`         |                $\uparrow$ | `\uparrow`                |
|     $\longrightarrow$ | `\longrightarrow`     |                $\Uparrow$ | `\Uparrow`                |
|        $\Updownarrow$ | `\Updownarrow`        |            $\updownarrow$ | `\updownarrow`            |

## Command Symbols

| Symbol | Command |
| ------ | ------- |
| $ $ $  | `\$`    |
| $\_$   | `\_`    |
| $\&$   | `\&`    |
| $\#$   | `\#`    |
| $\%$   | `\%`    |

## Brackets

| Symbol       | Command      |
| ------------ | ------------ |
| $\{$         | `\{`         |
| $\}$         | `\}`         |
| $\|$         | `\|`         |
| $\backslash$ | `\backslash` |
| $\langle$    | `\langle`    |
| $\lceil$     | `\lceil`     |
| $\lfloor$    | `\lfloor`    |
| $\rangle$    | `\rangle`    |
| $\rceil$     | `\rceil`     |
| $\rfloor$    | `\rfloor`    |

## Miscellaneous

|         Symbol | Command        |       Symbol | Command      |
| -------------: | -------------- | -----------: | ------------ |
|       $\aleph$ | `\aleph`       |        $\Im$ | `\Im`        |
|       $\angle$ | `\angle`       |   $\implies$ | `\implies`   |
|   $\backslash$ | `\backslash`   |        $\in$ | `\in`        |
|     $\bigstar$ | `\bigstar`     |     $\infty$ | `\infty`     |
| $\blacksquare$ | `\blacksquare` |      $\Join$ | `\Join`      |
|         $\bot$ | `\bot`         | $\mathbb{R}$ | `\mathbb{R}` |
|         $\Box$ | `\Box`         |       $\mho$ | `\mho`       |
|   $\checkmark$ | `\checkmark`   |     $\nabla$ | `\nabla`     |
|    $\clubsuit$ | `\clubsuit`    |   $\natural$ | `\natural`   |
|   $\copyright$ | `\copyright`   |       $\neg$ | `\neg`       |
|         $\cup$ | `\cup`         |         $\P$ | `\P`         |
|       $\dashv$ | `\dashv`       |   $\partial$ | `\partial`   |
|     $\Diamond$ | `\Diamond`     |    $\pounds$ | `\pounds`    |
| $\diamondsuit$ | `\diamondsuit` |     $\prime$ | `\prime`     |
| $\diamondsuit$ | `\diamondsuit` |        $\Re$ | `\Re`        |
|         $\ell$ | `\ell`         |         $\S$ | `\S`         |
|    $\emptyset$ | `\emptyset`    |     $\sharp$ | `\sharp`     |
|      $\exists$ | `\exists`      | $\spadesuit$ | `\spadesuit` |
|        $\flat$ | `\flat`        |    $\square$ | `\square`    |
|      $\forall$ | `\forall`      |      $\surd$ | `\surd`      |
|        $\hbar$ | `\hbar`        |       $\top$ | `\top`       |
|   $\heartsuit$ | `\heartsuit`   |  $\triangle$ | `\triangle`  |
|   $\heartsuit$ | `\heartsuit`   |     $\vdash$ | `\vdash`     |
|       $\Vdash$ | `\Vdash`       |     $\vDash$ | `\vDash`     |
|          $\wp$ | `\wp`          |              |              |

| $\$ | `\` |
| $\$ | `\` |
| $\$ | `\` |
| $\$ | `\` |

|     Symbol | Command    |     Symbol | Command    |
| ---------: | ---------- | ---------: | ---------- |
|   $\alpha$ | `\alpha`   |      $\Mu$ | `\Mu`      |
|   $\Alpha$ | `\Alpha`   |      $\nu$ | `\nu`      |
|    $\beta$ | `\beta`    |      $\Nu$ | `\Nu`      |
|    $\Beta$ | `\Beta`    |      $\xi$ | `\xi`      |
|   $\gamma$ | `\gamma`   |      $\Xi$ | `\Xi`      |
|   $\Gamma$ | `\Gamma`   | $\omicron$ | `\omicron` |
|   $\delta$ | `\delta`   | $\Omicron$ | `\Omicron` |
|   $\Delta$ | `\Delta`   |      $\pi$ | `\pi`      |
| $\epsilon$ | `\epsilon` |      $\Pi$ | `\Pi`      |
| $\Epsilon$ | `\Epsilon` |     $\rho$ | `\rho`     |
|    $\zeta$ | `\zeta`    |     $\Rho$ | `\Rho`     |
|    $\Zeta$ | `\Zeta`    |   $\sigma$ | `\sigma`   |
|     $\eta$ | `\eta`     |   $\Sigma$ | `\Sigma`   |
|     $\Eta$ | `\Eta`     |     $\tau$ | `\tau`     |
|   $\theta$ | `\theta`   |     $\Tau$ | `\Tau`     |
|   $\Theta$ | `\Theta`   | $\upsilon$ | `\upsilon` |
|   $\kappa$ | `\kappa`   | $\Upsilon$ | `\Upsilon` |
|   $\Kappa$ | `\Kappa`   |     $\phi$ | `\phi`     |
|  $\lambda$ | `\lambda`  |     $\Phi$ | `\Phi`     |
|  $\Lambda$ | `\Lambda`  |     $\chi$ | `\chi`     |
|      $\mu$ | `\mu`      |     $\Chi$ | `\Chi`     |
|   $\omega$ | `\omega`   |     $\psi$ | `\psi`     |
|   $\Omega$ | `\Omega`   |     $\Psi$ | `\Psi`     |

| Symbol   | Example                            | Script   |
| -------- | ---------------------------------- | -------- |
| $\cos$   | $\cos \beta = \frac{a}{c}$         | `\cos`   |
| $\sin$   | $\sin \alpha = \frac{a}{c}$        | `\sin`   |
| $\cot$   | $\cot \beta = \frac{a}{c}$         | `\cot`   |
| $\tan$   | $\tan \alpha = \frac{a}{c}$        | `\tan`   |
| $\lim$   | $\lim_{x\to\infty}(4)$             | `\lim`   |
| $\to$    | $\lim_{x\to\infty}(4)$             | `\to`    |
| $\infty$ | $\lim_{x\to\infty}(4)$             | `\infty` |
| $\sum$   | $\sum_{n=0}^{\infty}\frac{3}{2^n}$ | `\sum`   |
| $\int$   | $\int dx$                          | `\int`   |
| $\log$   | $\log_{2}(8)$                      | `\log`   |

| Symbol    | Command   |
| --------- | --------- |
| $\arccos$ | `\arccos` |
| $\arccot$ | `\arccot` |
| $\arccsc$ | `\arccsc` |
| $\arcsec$ | `\arcsec` |
| $\arcsin$ | `\arcsin` |
| $\arctan$ | `\arctan` |
| $\cos$    | `\cos`    |
| $\cosh$   | `\cosh`   |
| $\cot$    | `\cot`    |
| $\coth$   | `\coth`   |
| $\csc$    | `\csc`    |
| $\infty$  | `\infty`  |
| $\int$    | `\int`    |
| $\lim$    | `\lim`    |
| $\log$    | `\log`    |
| $\sec$    | `\sec`    |
| $\sin$    | `\sin`    |
| $\sinh$   | `\sinh`   |
| $\sum$    | `\sum`    |
| $\tan$    | `\tan`    |
| $\tanh$   | `\tanh`   |
| $\to$     | `\to`     |

| $\$ | `\` |
| $\$ | `\` |

⁡
| Symbol | Command | Symbol | Command |
|----------:|-----------|---------:|----------|
| $\arccos$ | `\arccos` | $\int$ | `\int` |
| $arccot$ | `\arccot` | $\lim$ | `\lim` |
| $arccsc$ | `\arccsc` | $\log$ | `\log` |
| $arcsec$ | `\arcsec` | $\sec$ | `\sec` |
| $\arcsin$ | `\arcsin` | $\sin$ | `\sin` |
| $\arctan$ | `\arctan` | $\sinh$ | `\sinh` |
| $\cos$ | `\cos` | $\sum$ | `\sum` |
| $\cosh$ | `\cosh` | $\tan$ | `\tan` |
| $\cot$ | `\cot` | $\tanh$ | `\tanh` |
| $\coth$ | `\coth` | $\to$ | `\to` |
| $\csc$ | `\csc` | $\infty$ | `\infty` |
⁡
⁡

